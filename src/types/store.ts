export type Store = {
	id: string | number;
	name: string;
	latitude: number;
	longitude: number;
	distance?: number;
	hours: string;
	phone: string;
	address: string;
	// Add other properties as needed
};
